# ТЗ: создание файлового менеджера CSharpFar

Source: `.worklog/0001.spec-csharpfar-file-manager.md`.

## 1. Цель проекта

Создать консольный двухпанельный файловый менеджер **CSharpFar** на C# и .NET 10, внешне и по основным сценариям похожий на Far Manager.

Проект должен быть пригоден для поэтапной разработки через Codex, Claude Code или аналогичный coding agent. Каждый этап должен давать рабочий результат, который можно запустить, проверить вручную, поправить и только после этого переходить к следующему этапу.

Главная цель первой версии: получить удобную консольную оболочку для Windows с двумя файловыми панелями, командной строкой, историей команд, базовыми файловыми операциями, просмотром и редактированием текстовых файлов.

## 2. Основные принципы

1. **Windows-first.** Основная целевая платформа: Windows Terminal / классическая консоль Windows / PowerShell / cmd.exe.
2. **.NET 10.** Целевой фреймворк: `net10.0`.
3. **C# only.** Основная логика пишется на C#.
4. **Без плагинной системы в первой версии.** Архитектура не должна мешать будущему расширению, но плагины не реализуются.
5. **Клавиатурное управление.** Основные сценарии должны работать без мыши.
6. **Пошаговая разработка.** После каждого этапа проект должен собираться, запускаться и иметь понятные критерии проверки.
7. **Far-like UX, но не полное клонирование.** Не нужно повторять все детали Far Manager. Нужно воспроизвести рабочую модель: две панели, командная строка, функциональные клавиши, истории, файловые операции, просмотрщик, редактор.
8. **Ctrl+O обязателен.** Нужно поддержать режим скрытия панелей, при котором под интерфейсом видно то, что было в консоли после выполнения shell-команд.

## 3. Не цели первой версии

В первой версии не нужно реализовывать:

1. плагины;
2. полноценную систему макросов Far Manager;
3. архивные панели;
4. FTP/SFTP;
5. сложную систему прав доступа;
6. полноценный hex editor;
7. полноценный IDE-like редактор кода;
8. полную совместимость с Far Manager по горячим клавишам;
9. поддержку всех терминалов Linux/macOS на уровне Windows.

Эти возможности можно оставить как backlog после стабильного MVP.

## 4. Технологический стек

### 4.1. Платформа

* C#
* .NET 10
* Windows-first console application
* Nullable reference types enabled
* Treat warnings as errors желательно включить после стабилизации базовой архитектуры

### 4.2. Рекомендуемый тип проекта

Создать solution:

```text
CSharpFar.sln
/src
  /CSharpFar.App
  /CSharpFar.Core
  /CSharpFar.Console
  /CSharpFar.FileSystem
  /CSharpFar.Shell
  /CSharpFar.Viewer
  /CSharpFar.Editor
/tests
  /CSharpFar.Tests
```

Минимально допустимо начать с меньшего количества проектов, но код должен быть разделён по слоям так, чтобы UI, файловая модель, shell-интеграция и операции над файлами не были свалены в один `Program.cs`.

### 4.3. Внешние зависимости

На старте желательно использовать минимум зависимостей.

Разрешены:

* стандартная библиотека .NET;
* `Microsoft.Extensions.*` для DI, logging и configuration;
* `System.Text.Json`;
* `Microsoft.Data.Sqlite` только если будет принято решение хранить историю и настройки в SQLite;
* тестовые библиотеки `xUnit` или `NUnit`.

Не использовать на первом этапе:

* Terminal.Gui;
* Spectre.Console как основной UI-движок;
* полноценные сторонние редакторы;
* сторонние файловые менеджеры или TUI-фреймворки.

Причина: для режима `Ctrl+O` нужен точный контроль над console buffer, теневым display buffer и способом запуска shell-команд. Сторонние TUI-фреймворки могут усложнить это поведение.

## 5. Архитектурная идея

Приложение состоит из следующих подсистем:

```text
App Host
  ├── Input loop
  ├── Screen renderer
  ├── Panel model
  ├── Command line model
  ├── File operation service
  ├── Shell execution service
  ├── Viewer
  ├── Editor
  ├── History service
  └── Configuration service
```

### 5.1. Слои

#### `CSharpFar.Core`

Содержит доменные модели и контракты:

* `FilePanelState`
* `FilePanelItem`
* `PanelSide`
* `SortMode`
* `SelectionState`
* `CommandHistoryItem`
* `DirectoryHistoryItem`
* `IFileSystemService`
* `IFileOperationService`
* `IShellService`
* `IHistoryStore`
* `ISettingsStore`

#### `CSharpFar.Console`

Содержит работу с консолью:

* ввод клавиш;
* отрисовка экрана через теневой display buffer и diff flush;
* управление цветами;
* чтение размера консоли;
* очистка областей;
* Windows-specific screen buffer operations для режима `Ctrl+O`.

#### `CSharpFar.FileSystem`

Содержит:

* чтение каталогов;
* сортировку;
* фильтрацию;
* создание папок;
* удаление;
* копирование;
* перемещение;
* обработку конфликтов;
* прогресс файловых операций.

#### `CSharpFar.Shell`

Содержит:

* запуск команд через `cmd.exe` или `powershell.exe`;
* выбор shell по настройке;
* работу с текущим каталогом активной панели;
* сохранение истории команд;
* режим отображения shell output под панелями.

#### `CSharpFar.Viewer`

Содержит внутренний просмотрщик:

* текстовый режим;
* постраничное чтение;
* поиск;
* кодировки;
* режим больших файлов.

#### `CSharpFar.Editor`

Содержит простой текстовый редактор:

* загрузка файла;
* редактирование;
* сохранение;
* dirty-state;
* поиск;
* базовая отмена действий, если это не сильно усложняет этап.

## 6. Ключевое требование: режим Ctrl+O

### 6.1. Поведение

`Ctrl+O` переключает режим видимости панелей:

* если панели видимы, они скрываются;
* пользователь видит содержимое консоли, которое было под интерфейсом;
* командная строка и панели не должны затирать этот вывод;
* повторное `Ctrl+O` возвращает панели;
* состояние панелей, курсор, выделение и текущие каталоги сохраняются.

Это один из принципиальных сценариев Far Manager. Для CSharpFar он должен быть заложен в архитектуру с самого начала.

### 6.2. Как должны выполняться shell-команды

Когда пользователь вводит команду в нижней строке:

1. приложение временно скрывает панели или освобождает область вывода;
2. команда запускается в текущем каталоге активной панели;
3. stdout/stderr команды выводятся в тот же console buffer, а не только в отдельный внутренний лог;
4. после завершения команды панели снова рисуются поверх консоли;
5. если пользователь нажимает `Ctrl+O`, он видит вывод выполненных команд.

### 6.3. Техническая реализация для Windows

Нужен отдельный слой `IConsoleDriver`:

```csharp
public interface IConsoleDriver
{
    ConsoleSize GetSize();
    ConsoleKeyInfo ReadKey(bool intercept);
    void WriteAt(int x, int y, ReadOnlySpan<char> text, ConsoleColor? foreground = null, ConsoleColor? background = null);
    void ClearRegion(Rect region);
    void SetCursorPosition(int x, int y);
    void SetCursorVisible(bool visible);
    ScreenSnapshot Capture(Rect region);
    void Restore(ScreenSnapshot snapshot);
}
```

Для Windows-реализации желательно использовать P/Invoke к Win32 Console API там, где стандартного `System.Console` недостаточно:

* чтение/сохранение участка screen buffer;
* восстановление участка screen buffer;
* управление курсором;
* корректная работа с цветами.

`ScreenRenderer` должен владеть теневым буфером отображения: UI рисует полный логический кадр, renderer сравнивает новый кадр с предыдущим и физически обновляет только изменившиеся горизонтальные участки. Код приложения не должен вычислять и передавать dirty regions для обычной перерисовки; `Rect` остаётся геометрией компонентов и областей `Capture`/`Restore`, а не протоколом инкрементального repaint.

Минимальный MVP может временно использовать упрощённый подход:

* перед отрисовкой панелей сохранять собственный underlay buffer;
* при `Ctrl+O` восстанавливать последнюю сохранённую подложку;
* после выполнения shell-команды обновлять подложку.

Но архитектурно код должен быть готов к полноценной Windows screen buffer реализации.

### 6.4. Что нельзя делать

Нельзя реализовывать `Ctrl+O` как простую очистку экрана.

Нельзя запускать команды только с redirected output, если из-за этого пользователь потом не видит реальный вывод в консоли под панелями.

Нельзя использовать alternate screen buffer как единственный экранный режим, если это ломает сценарий “панели скрылись, под ними виден shell output”.

## 7. Основной пользовательский интерфейс

### 7.1. Структура экрана

По умолчанию экран состоит из:

```text
┌────────────────────────────┐┌────────────────────────────┐
│ Left panel                 ││ Right panel                │
│                            ││                            │
│ file list                  ││ file list                  │
│                            ││                            │
└────────────────────────────┘└────────────────────────────┘
C:\current\path> command line
1Help 2User 3View 4Edit 5Copy 6RenMov 7Mkdir 8Delete 9Menu 10Quit
```

Точная псевдографика может отличаться, но визуально это должен быть двухпанельный console file manager.

### 7.2. Панели

Каждая панель должна показывать:

* текущий путь;
* список файлов и каталогов;
* имя;
* размер;
* дату изменения;
* атрибуты, если помещаются;
* выделение активной строки;
* выбранные файлы;
* строку `..` для перехода в родительский каталог.

### 7.3. Активная панель

* `Tab` переключает активную панель.
* Активная панель визуально отличается от неактивной.
* Все операции по умолчанию выполняются из активной панели в неактивную.

### 7.4. Навигация

Обязательные клавиши:

| Клавиша               | Действие                                                                               |
| --------------------- | -------------------------------------------------------------------------------------- |
| `Up` / `Down`         | Перемещение курсора                                                                    |
| `PageUp` / `PageDown` | Постраничная прокрутка                                                                 |
| `Home` / `End`        | Начало / конец списка                                                                  |
| `Enter` на каталоге   | Войти в каталог                                                                        |
| `Enter` на файле      | Выполнить файл или открыть по системной ассоциации, можно отложить до отдельного этапа |
| `Backspace`           | Перейти в родительский каталог                                                         |
| `Tab`                 | Переключить панель                                                                     |
| `Ctrl+O`              | Скрыть / показать панели                                                               |
| `Esc`                 | Закрыть текущий диалог или режим                                                       |

### 7.5. Функциональные клавиши

Минимальная раскладка:

| Клавиша   | Действие                                         |
| --------- | ------------------------------------------------ |
| `F1`      | Help, можно заглушку на первых этапах            |
| `F2`      | User menu, можно заглушку на первых этапах       |
| `F3`      | View                                             |
| `F4`      | Edit                                             |
| `F5`      | Copy                                             |
| `F6`      | Rename / Move                                    |
| `F7`      | MkDir                                            |
| `F8`      | Delete                                           |
| `F9`      | Menu / Settings, можно заглушку на первых этапах |
| `F10`     | Exit                                             |
| `Alt+F8`  | Command history                                  |
| `Alt+F11` | View/Edit history                                |
| `Alt+F12` | Directory history                                |

## 8. Командная строка

### 8.1. Поведение

В нижней части экрана должна быть командная строка.

Требования:

* ввод обычного текста;
* `Enter` запускает команду;
* команда выполняется в каталоге активной панели;
* после выполнения команда добавляется в историю;
* история сохраняется между запусками приложения;
* `Alt+F8` открывает окно истории команд;
* выбранная команда из истории вставляется обратно в командную строку;
* `Up` / `Down` в командной строке могут листать историю, если это не конфликтует с навигацией по панели.

### 8.2. Shell

Настройка shell:

```json
{
  "shell": {
    "kind": "cmd",
    "executable": "cmd.exe",
    "argumentsFormat": "/c {0}"
  }
}
```

В будущем можно добавить PowerShell:

```json
{
  "shell": {
    "kind": "powershell",
    "executable": "powershell.exe",
    "argumentsFormat": "-NoLogo -NoProfile -Command {0}"
  }
}
```

### 8.3. Вывод команд

Вывод shell-команд должен попадать в console buffer так, чтобы его можно было увидеть через `Ctrl+O`.

Для MVP допустимо:

* перед запуском команды временно скрыть панели;
* вывести приглашение и команду;
* дождаться завершения процесса;
* оставить вывод в консоли;
* затем перерисовать панели поверх.

После `Ctrl+O` этот вывод должен быть виден пользователю.

## 9. Файловая модель панели

### 9.1. `FilePanelItem`

Минимальная модель:

```csharp
public sealed class FilePanelItem
{
    public required string Name { get; init; }
    public required string FullPath { get; init; }
    public required bool IsDirectory { get; init; }
    public long? Size { get; init; }
    public DateTime LastWriteTime { get; init; }
    public FileAttributes Attributes { get; init; }
    public bool IsParentDirectory { get; init; }
}
```

### 9.2. `FilePanelState`

```csharp
public sealed class FilePanelState
{
    public required string CurrentDirectory { get; set; }
    public List<FilePanelItem> Items { get; } = new();
    public HashSet<string> SelectedPaths { get; } = new(StringComparer.OrdinalIgnoreCase);
    public int CursorIndex { get; set; }
    public int ScrollOffset { get; set; }
    public SortMode SortMode { get; set; }
    public bool SortDescending { get; set; }
    public string? FilterMask { get; set; }
}
```

### 9.3. Сортировка

Обязательные режимы:

* name;
* extension;
* size;
* last write time.

Каталоги по умолчанию показываются перед файлами.

### 9.4. Выделение

Минимальные операции:

| Клавиша               | Действие                                                    |
| --------------------- | ----------------------------------------------------------- |
| `Insert`              | Выделить / снять выделение текущего элемента и перейти вниз |
| `Gray *` или `Ctrl+A` | Выделить / снять выделение всех файлов на панели            |
| `Esc`                 | Снять выделение, если нет активного диалога                 |

Операции копирования, перемещения и удаления работают с выделенными файлами. Если выделения нет, операция применяется к элементу под курсором.

## 10. Файловые операции

### 10.1. Общие требования

Все операции должны:

* работать с одним файлом;
* работать с выделенной группой файлов;
* обрабатывать ошибки доступа;
* показывать понятные сообщения об ошибках;
* не падать от исключений `IOException`, `UnauthorizedAccessException`, `PathTooLongException`;
* иметь возможность отмены через `Esc` или `CancellationToken`, где это применимо.

### 10.2. Копирование `F5`

При нажатии `F5`:

1. определить список источников: выделение или текущий элемент;
2. открыть диалог назначения;
3. по умолчанию предложить путь текущего каталога противоположной панели;
4. выполнить копирование;
5. показать прогресс;
6. обновить обе панели после завершения.

Требования:

* рекурсивное копирование каталогов;
* сохранение структуры каталогов;
* обработка конфликтов:

  * overwrite;
  * skip;
  * overwrite all;
  * skip all;
  * cancel;
* отображение текущего файла;
* отображение общего прогресса по количеству файлов и по байтам, если размер заранее посчитан;
* корректная работа с большими файлами через потоковое копирование.

### 10.3. Перемещение / переименование `F6`

При нажатии `F6`:

* если выбран один файл и пользователь вводит новое имя без пути, выполнить rename;
* если указан путь, выполнить move;
* если выбрано несколько файлов, выполнить move в указанный каталог;
* по умолчанию предложить каталог противоположной панели.

### 10.4. Создание папки `F7`

* открыть маленький input dialog;
* создать папку в каталоге активной панели;
* если папка уже существует, показать ошибку;
* после создания обновить панель и поставить курсор на новую папку.

### 10.5. Удаление `F8`

Минимальное поведение:

* запросить подтверждение;
* удалить файл или выделенную группу;
* для каталогов выполнять рекурсивное удаление;
* после удаления обновить панель.

Опционально для Windows:

* удаление в корзину через Windows API;
* `Shift+Del` удаляет без корзины.

В MVP можно реализовать физическое удаление с явным предупреждением.

## 11. Внутренний просмотрщик `F3`

### 11.1. MVP

Просмотрщик должен:

* открывать файл из активной панели;
* показывать текст в полноэкранном режиме внутри консоли;
* поддерживать прокрутку вверх/вниз;
* поддерживать `PageUp`, `PageDown`, `Home`, `End`;
* закрываться по `Esc` или `F10`;
* не изменять файл.

### 11.2. Кодировки

Минимально:

* UTF-8 with BOM/no BOM;
* UTF-16 LE with BOM;
* system default encoding для Windows, если возможно.

Следующий уровень:

* Windows-1251;
* ручной выбор кодировки через диалог.

### 11.3. Большие файлы

Для MVP допустимо ограничить размер файла для полного чтения, например 20 MB.

Если файл больше лимита:

* показать предупреждение;
* читать файл постранично или открыть в streaming viewer;
* не загружать гигабайтный файл целиком в память.

### 11.4. Поиск

На следующем этапе:

* `F7` или `/` открывает поиск;
* поиск вперёд;
* переход к следующему совпадению;
* подсветка текущего совпадения, если это не усложняет рендер.

## 12. Встроенный редактор `F4`

### 12.1. MVP

Редактор должен:

* открывать текстовый файл;
* показывать содержимое в полноэкранном режиме;
* позволять вводить текст;
* поддерживать Backspace, Delete, Enter, стрелки;
* сохранять файл по `F2`;
* закрываться по `Esc` или `F10`;
* если есть несохранённые изменения, спрашивать подтверждение.

### 12.2. Модель текста

Для первой реализации можно использовать простую модель:

```csharp
List<string> lines
int cursorLine
int cursorColumn
int scrollLine
int horizontalOffset
bool isDirty
```

Не нужно на первом этапе делать rope/piece table. Это можно добавить позже, если редактор станет узким местом.

### 12.3. Line endings и кодировки

При загрузке файла нужно определить:

* `CRLF`;
* `LF`;
* последнюю пустую строку;
* BOM, если есть.

При сохранении желательно сохранять исходный стиль line endings и BOM.

### 12.4. Ограничения MVP

В MVP редактор может отказаться открывать очень большие файлы, например больше 10 MB, с понятным сообщением.

## 13. Истории

### 13.1. Виды историй

Нужно поддержать:

1. историю команд: `Alt+F8`;
2. историю открытых файлов viewer/editor: `Alt+F11`;
3. историю каталогов: `Alt+F12`.

### 13.2. Хранение

Для начала использовать JSON-файлы в пользовательском профиле:

```text
%APPDATA%\CSharpFar\settings.json
%APPDATA%\CSharpFar\command-history.json
%APPDATA%\CSharpFar\directory-history.json
%APPDATA%\CSharpFar\file-history.json
```

Portable mode:

```text
./CSharpFar.config/settings.json
./CSharpFar.config/command-history.json
```

Portable mode включается, если рядом с exe есть файл:

```text
CSharpFar.portable
```

### 13.3. Формат истории команд

```json
[
  {
    "command": "dotnet build",
    "workingDirectory": "C:\\Projects\\Demo",
    "timestampUtc": "2026-05-06T12:00:00Z"
  }
]
```

### 13.4. Диалоги истории

Окно истории должно позволять:

* выбрать запись;
* вставить её в командную строку;
* удалить запись, можно отложить;
* закрыть окно без выбора.

## 14. Настройки

### 14.1. `settings.json`

Пример:

```json
{
  "ui": {
    "theme": "classic-blue",
    "showHiddenFiles": true,
    "showSystemFiles": true,
    "confirmDelete": true
  },
  "shell": {
    "kind": "cmd",
    "executable": "cmd.exe",
    "argumentsFormat": "/c {0}"
  },
  "panels": {
    "leftStartDirectory": null,
    "rightStartDirectory": null,
    "defaultSortMode": "name"
  },
  "history": {
    "maxCommandHistoryItems": 1000,
    "maxDirectoryHistoryItems": 500,
    "maxFileHistoryItems": 500
  }
}
```

### 14.2. Цветовые схемы

В MVP достаточно встроенной classic-схемы:

* синий фон панелей;
* белый текст;
* яркое выделение активной строки;
* отличимый цвет активной панели.

Позже добавить пользовательские темы в JSON.

## 15. Диалоги

Нужен минимальный набор стандартных диалогов:

1. message box;
2. confirmation box;
3. input box;
4. list box;
5. progress dialog.

Диалоги должны быть частью собственного console UI слоя, а не зависеть от сторонней TUI-библиотеки.

## 16. Обработка ошибок

Приложение не должно падать при обычных ошибках файловой системы.

Ошибки показывать пользователю в диалоге:

* нет доступа;
* файл уже существует;
* файл занят другим процессом;
* путь не найден;
* слишком длинный путь;
* ошибка чтения каталога;
* ошибка копирования;
* ошибка удаления.

Для неожиданных ошибок:

* писать лог в файл;
* показывать короткое сообщение;
* по возможности продолжать работу.

Логи:

```text
%APPDATA%\CSharpFar\logs\csharpfar-yyyyMMdd.log
```

## 17. Тестирование

### 17.1. Unit tests

Покрыть тестами:

* сортировку панели;
* фильтрацию;
* выбор файлов;
* построение списка операций копирования;
* обработку конфликтов;
* загрузку/сохранение настроек;
* историю команд;
* модель редактора.

### 17.2. Integration tests

По возможности:

* создать временную файловую структуру;
* выполнить копирование;
* выполнить перемещение;
* выполнить удаление;
* проверить результат на диске.

### 17.3. UI tests

На старте не требуются полноценные UI tests.

Но архитектура input/render loop должна позволять подставить fake console driver и проверять реакции на клавиши без реальной консоли.

## 18. Сборка и публикация

### 18.1. Команды

```bash
dotnet build
```

```bash
dotnet test
```

```bash
dotnet publish src/CSharpFar.App/CSharpFar.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

### 18.2. Результат публикации

Ожидаемый результат:

```text
CSharpFar.exe
```

Приложение должно запускаться из любой папки и открывать панели в текущем рабочем каталоге.

## 19. Поэтапный план разработки

Каждый этап ниже является отдельной задачей для Codex / Claude Code.

Общее правило для каждого этапа:

1. реализовать только требования текущего этапа;
2. не забегать в следующие этапы;
3. после реализации обновить `README.md` коротким описанием текущего состояния;
4. добавить или обновить тесты, если этап затрагивает тестируемую логику;
5. запустить `dotnet build` и `dotnet test`;
6. остановиться и сообщить, что этап завершён.

## Этап 0. Создание solution и каркаса проекта

### Цель

Создать пустой, но правильно структурированный проект CSharpFar.

### Нужно сделать

1. Создать `CSharpFar.sln`.
2. Создать проекты:

   * `CSharpFar.App`;
   * `CSharpFar.Core`;
   * `CSharpFar.Console`;
   * `CSharpFar.FileSystem`;
   * `CSharpFar.Shell`;
   * `CSharpFar.Tests`.
3. Настроить target framework `net10.0`.
4. Включить nullable reference types.
5. Добавить базовый `README.md`.
6. Добавить `.editorconfig`.
7. Добавить первый smoke test.

### Критерии приёмки

* `dotnet build` проходит без ошибок.
* `dotnet test` проходит без ошибок.
* `dotnet run --project src/CSharpFar.App` запускает приложение и выводит короткое сообщение-заглушку.

### Stop point

После этого этапа остановиться. Не реализовывать UI.

## Этап 1. Console abstraction и базовый renderer

### Цель

Создать слой работы с консолью, чтобы UI не зависел напрямую от `System.Console`.

### Нужно сделать

1. Создать интерфейс `IConsoleDriver`.
2. Реализовать `SystemConsoleDriver`.
3. Добавить модели:

   * `ConsoleSize`;
   * `Rect`;
   * `CellStyle`;
   * `ScreenSnapshot`.
4. Добавить базовый `ScreenRenderer`.
5. Реализовать очистку экрана, запись текста в координаты, чтение клавиши.
6. Добавить теневой display buffer в `ScreenRenderer`: begin frame, запись в back buffer, flush только изменившихся ячеек.
7. Не вводить ручную передачу dirty regions из `Application`; приложение рисует полный логический кадр, renderer сам вычисляет физический diff.
8. Добавить fake console driver для тестов.

### Критерии приёмки

* Можно написать текст в заданную координату.
* Можно очистить область экрана.
* Можно получить размер консоли.
* Повторная отрисовка одинакового кадра не делает физических записей в console driver.
* Есть тесты для fake console driver и простого renderer.

### Stop point

После этого этапа остановиться. Не реализовывать панели.

## Этап 2. Две панели без файловых операций

### Цель

Показать на экране две панели с содержимым каталогов.

### Нужно сделать

1. Реализовать `FilePanelState` и `FilePanelItem`.
2. Реализовать чтение текущего каталога.
3. Нарисовать две панели.
4. Добавить строку функциональных клавиш снизу.
5. Добавить переключение активной панели по `Tab`.
6. Добавить навигацию `Up`, `Down`, `PageUp`, `PageDown`, `Home`, `End`.
7. Добавить вход в каталог по `Enter`.
8. Добавить переход в родительский каталог по `Backspace` и через элемент `..`.
9. Добавить выход по `F10`.

### Критерии приёмки

* При запуске видны две панели.
* Обе панели открываются в текущем рабочем каталоге.
* Активная панель визуально отличается.
* Можно перемещаться по списку файлов.
* Можно входить в каталоги и возвращаться назад.
* `F10` завершает приложение.

### Stop point

После этого этапа остановиться. Не реализовывать копирование, удаление и command line.

## Этап 3. Командная строка и выполнение shell-команд

### Цель

Добавить нижнюю командную строку и запуск команд в shell.

### Нужно сделать

1. Добавить модель командной строки.
2. Поддержать ввод текста.
3. Поддержать Backspace, Delete, Left, Right, Home, End.
4. Запускать команду по `Enter`.
5. Команда должна выполняться в текущем каталоге активной панели.
6. Для MVP использовать `cmd.exe /c`.
7. Перед запуском команды временно скрывать панели или освобождать область вывода.
8. После завершения команды перерисовывать панели.
9. Добавить запись команды в in-memory историю.

### Критерии приёмки

* Можно ввести `dir` и увидеть вывод команды.
* Команда выполняется в каталоге активной панели.
* После завершения команды панели возвращаются.
* Приложение не ломает навигацию после выполнения команды.

### Stop point

После этого этапа остановиться. Историю на диск и `Ctrl+O` пока не делать.

## Этап 4. Ctrl+O и underlay console output

### Цель

Реализовать Far-like режим скрытия панелей.

### Нужно сделать

1. Добавить состояние `PanelsVisible`.
2. Обработать `Ctrl+O`.
3. При скрытии панелей восстановить underlay / console snapshot.
4. При повторном `Ctrl+O` снова нарисовать панели.
5. Обеспечить, чтобы вывод shell-команд оставался виден после скрытия панелей.
6. Подготовить Windows-specific реализацию snapshot через отдельный класс, даже если первая реализация будет упрощённой.

### Критерии приёмки

* Пользователь запускает команду, например `dir`.
* После завершения команды панели снова видны.
* Пользователь нажимает `Ctrl+O`.
* Панели скрываются.
* На экране виден вывод `dir`, а не пустой экран.
* Повторный `Ctrl+O` возвращает панели.
* Навигационное состояние панелей сохраняется.

### Stop point

После этого этапа остановиться. Это важная архитектурная точка. Не продолжать, пока поведение `Ctrl+O` не проверено вручную.

## Этап 5. Выделение файлов и сортировка

### Цель

Добавить базовую работу со списком файлов.

### Нужно сделать

1. `Insert` выделяет / снимает выделение текущего элемента.
2. После `Insert` курсор переходит вниз.
3. `Ctrl+A` выделяет все обычные элементы панели или снимает выделение, если уже всё выделено.
4. Добавить сортировку:

   * по имени;
   * по расширению;
   * по размеру;
   * по дате изменения.
5. Добавить горячие клавиши сортировки, можно временные:

   * `Ctrl+F3` name;
   * `Ctrl+F4` extension;
   * `Ctrl+F5` last write time;
   * `Ctrl+F6` size.
6. Добавить отображение выбранных файлов.

### Критерии приёмки

* Можно выделять один или несколько файлов.
* Выделение видно на панели.
* Сортировка меняет порядок элементов.
* Каталоги остаются перед файлами, если не задано другое поведение.

### Stop point

После этого этапа остановиться.

## Этап 6. Создание папки `F7`

### Цель

Реализовать первый modal dialog и простую файловую операцию.

### Нужно сделать

1. Добавить input dialog.
2. По `F7` открыть диалог имени папки.
3. Создать папку в текущем каталоге активной панели.
4. Обновить панель после создания.
5. Поставить курсор на созданную папку.
6. Показать ошибку, если папка уже существует или нет доступа.

### Критерии приёмки

* `F7` создаёт папку.
* Ошибки показываются в диалоге.
* После операции панель обновляется.

### Stop point

После этого этапа остановиться.

## Этап 7. Копирование `F5`

### Цель

Реализовать копирование файлов и каталогов.

### Нужно сделать

1. По `F5` определить источники: выделенные элементы или текущий элемент.
2. Открыть dialog назначения.
3. По умолчанию подставить каталог противоположной панели.
4. Реализовать копирование одного файла.
5. Реализовать копирование нескольких файлов.
6. Реализовать рекурсивное копирование каталога.
7. Добавить progress dialog.
8. Добавить обработку конфликтов:

   * overwrite;
   * skip;
   * cancel.
9. После копирования обновить обе панели.

### Критерии приёмки

* Можно скопировать файл в другую панель.
* Можно скопировать каталог рекурсивно.
* Можно скопировать несколько выделенных файлов.
* При конфликте имени появляется понятный вопрос.
* Ошибка копирования одного файла не роняет приложение.

### Stop point

После этого этапа остановиться. Не делать move/delete в этом же этапе.

## Этап 8. Перемещение и переименование `F6`

### Цель

Реализовать move/rename.

### Нужно сделать

1. По `F6` открыть dialog назначения.
2. Если один элемент и введено новое имя без пути, выполнить rename.
3. Если введён путь, выполнить move.
4. Если выбрано несколько элементов, переместить их в указанный каталог.
5. Обработать конфликты имён.
6. Обновить панели.

### Критерии приёмки

* Можно переименовать файл.
* Можно переименовать каталог.
* Можно переместить файл в каталог другой панели.
* Можно переместить несколько выделенных файлов.

### Stop point

После этого этапа остановиться.

## Этап 9. Удаление `F8`

### Цель

Реализовать удаление файлов и каталогов.

### Нужно сделать

1. По `F8` определить выбранные элементы или текущий элемент.
2. Показать confirmation dialog.
3. Удалить файл.
4. Удалить каталог рекурсивно.
5. Обработать ошибки доступа.
6. Обновить панель.
7. Опционально добавить `Shift+Del` как force delete.

### Критерии приёмки

* Удаление требует подтверждения.
* Можно удалить файл.
* Можно удалить каталог.
* Можно удалить выделенную группу.
* Ошибки не роняют приложение.

### Stop point

После этого этапа остановиться.

## Этап 10. Persisted command history и `Alt+F8`

### Цель

Сохранять историю команд между запусками.

### Нужно сделать

1. Добавить `IHistoryStore`.
2. Реализовать JSON-хранилище истории.
3. Сохранять команды после выполнения.
4. Загружать историю при старте.
5. По `Alt+F8` показывать list dialog.
6. Выбранную команду вставлять в command line.

### Критерии приёмки

* Команды сохраняются после перезапуска приложения.
* `Alt+F8` показывает историю.
* Выбранная команда вставляется в строку ввода.

### Stop point

После этого этапа остановиться.

## Этап 11. Directory history `Alt+F12`

### Цель

Сохранять историю посещённых каталогов.

### Нужно сделать

1. Добавлять каталог в историю при переходе.
2. Сохранять историю на диск.
3. По `Alt+F12` показывать list dialog.
4. При выборе каталога активная панель переходит в него.
5. Если каталог больше не существует, показать ошибку и предложить удалить запись, можно позже.

### Критерии приёмки

* История каталогов сохраняется.
* `Alt+F12` открывает список.
* Выбор записи меняет каталог активной панели.

### Stop point

После этого этапа остановиться.

## Этап 12. Просмотрщик `F3`

### Цель

Добавить внутренний просмотр текстовых файлов.

### Нужно сделать

1. По `F3` открыть файл под курсором.
2. Если выбран каталог, ничего не делать или показать сообщение.
3. Открыть полноэкранный viewer.
4. Поддержать прокрутку.
5. Поддержать `Esc` / `F10` для выхода.
6. Добавить простое определение кодировки.
7. Добавить лимит размера файла для MVP.
8. Добавлять файл в file history.

### Критерии приёмки

* Можно открыть `.txt`, `.cs`, `.json`, `.md`.
* Можно прокручивать файл.
* Выход возвращает в файловые панели.
* После выхода состояние панелей сохранено.

### Stop point

После этого этапа остановиться.

## Этап 13. Редактор `F4`

### Цель

Добавить простой встроенный текстовый редактор.

### Нужно сделать

1. По `F4` открыть файл под курсором.
2. Если файл не существует, можно создать новый через отдельный сценарий позже.
3. Загружать текст в модель `List<string>`.
4. Поддержать перемещение курсора.
5. Поддержать ввод символов.
6. Поддержать Backspace, Delete, Enter.
7. `F2` сохраняет файл.
8. `Esc` / `F10` закрывает редактор.
9. Если есть изменения, спрашивать сохранить / выйти без сохранения / cancel.
10. Добавлять файл в file history.

### Критерии приёмки

* Можно открыть текстовый файл.
* Можно изменить текст.
* Можно сохранить по `F2`.
* При выходе с изменениями появляется вопрос.
* После закрытия редактора панели работают.

### Stop point

После этого этапа остановиться.

## Этап 14. File history `Alt+F11`

### Цель

Добавить историю просмотренных и редактированных файлов.

### Нужно сделать

1. Сохранять файл после открытия через viewer или editor.
2. По `Alt+F11` показывать список файлов.
3. При выборе файла предложить открыть viewer или editor.
4. Если файл не существует, показать ошибку.

### Критерии приёмки

* `Alt+F11` показывает историю файлов.
* Можно открыть файл из истории.

### Stop point

После этого этапа остановиться.

## Этап 15. Настройки и portable mode

### Цель

Добавить конфигурацию приложения.

### Нужно сделать

1. Добавить `settings.json`.
2. Загружать настройки при старте.
3. Создавать настройки по умолчанию, если файла нет.
4. Поддержать `%APPDATA%\CSharpFar`.
5. Поддержать portable mode через файл `CSharpFar.portable` рядом с exe.
6. Вынести настройки shell, истории, показа hidden/system files.

### Критерии приёмки

* Приложение создаёт настройки при первом запуске.
* Настройки читаются при следующем запуске.
* Portable mode сохраняет файлы рядом с приложением.

### Stop point

После этого этапа остановиться.

## Этап 16. Быстрый просмотр `Ctrl+Q`

### Цель

Добавить быстрый просмотр файла в противоположной панели или отдельной области.

### Нужно сделать

1. По `Ctrl+Q` включать / выключать quick view mode.
2. В quick view mode неактивная панель показывает содержимое файла под курсором активной панели.
3. При перемещении курсора quick view обновляется.
4. Для каталогов показывать краткую информацию: путь, количество элементов можно позже.
5. Для больших файлов показывать ограниченный preview.

### Критерии приёмки

* `Ctrl+Q` включает быстрый просмотр.
* При выборе другого файла preview меняется.
* Повторный `Ctrl+Q` возвращает обычную вторую панель.

### Stop point

После этого этапа остановиться.

## Этап 17. Поиск файлов `Alt+F7`

### Цель

Добавить простой поиск файлов.

### Нужно сделать

1. По `Alt+F7` открыть search dialog.
2. Ввести маску, например `*.cs`.
3. Искать от текущего каталога активной панели.
4. Показывать результаты в list dialog.
5. При выборе результата перейти к файлу в панели.

### Критерии приёмки

* Можно найти файл по маске.
* Можно перейти к найденному файлу.
* Поиск можно отменить.

### Stop point

После этого этапа остановиться.

## Этап 18. User menu `F2`

### Цель

Добавить простое пользовательское меню команд.

### Нужно сделать

1. Добавить файл `user-menu.json`.
2. По `F2` показывать список команд.
3. Команда может содержать placeholders:

   * `{current}` текущий файл;
   * `{selected}` список выбранных файлов;
   * `{panelDir}` текущий каталог активной панели;
   * `{otherPanelDir}` каталог противоположной панели.
4. Запускать выбранную команду через shell service.
5. Добавлять выполненную команду в историю.

### Критерии приёмки

* `F2` показывает меню.
* Можно выполнить команду из меню.
* Placeholders подставляются корректно.

### Stop point

После этого этапа остановиться.

## Этап 19. Help `F1` и документация

### Цель

Добавить встроенную справку и нормальное описание проекта.

### Нужно сделать

1. По `F1` открыть help viewer.
2. Описать основные клавиши.
3. Обновить `README.md`.
4. Добавить раздел build/run/publish.
5. Добавить known limitations.

### Критерии приёмки

* `F1` показывает справку.
* README объясняет, как собрать и запустить проект.

### Stop point

После этого этапа остановиться.

## 20. Backlog после MVP

После прохождения этапов выше можно рассмотреть:

1. удаление в корзину Windows;
2. полноценный Windows Console API driver;
3. поддержку PowerShell как shell по умолчанию;
4. поиск и замену в редакторе;
5. streaming viewer для очень больших файлов;
6. hex view;
7. сравнение каталогов;
8. синхронизацию панелей;
9. виртуальную временную панель;
10. цветовые темы;
11. локализацию RU/EN;
12. макросы клавиш;
13. поддержку мыши;
14. Linux/macOS terminal driver;
15. single-file publishing pipeline;
16. GitHub Actions build/test/release.

## 21. Инструкция для coding agent

При работе с этим ТЗ соблюдать следующие правила:

1. Не реализовывать несколько этапов за один проход без явного разрешения пользователя.
2. Перед изменениями посмотреть текущую структуру проекта.
3. Сохранять архитектурные границы между слоями.
4. Не помещать всю логику в `Program.cs`.
5. Не добавлять тяжёлые зависимости без необходимости.
6. Для каждого этапа обновлять README коротким changelog-разделом.
7. Для каждого этапа запускать:

```bash
dotnet build
dotnet test
```

8. Если тесты не проходят, остановиться и описать проблему.
9. Если требование неоднозначно, выбрать самый простой вариант, не блокирующий следующий этап, и явно описать принятое решение.
10. Особенно осторожно работать с `Ctrl+O`, shell output и console buffer. Не заменять это простой очисткой экрана.

## 22. Минимальный Definition of Done для MVP

MVP считается готовым, если:

1. приложение запускается как console app на Windows;
2. отображаются две панели;
3. можно перемещаться по каталогам;
4. работает командная строка;
5. работает `Ctrl+O` и показывает shell output под панелями;
6. работают `F5`, `F6`, `F7`, `F8`;
7. работает история команд `Alt+F8`;
8. работает история каталогов `Alt+F12`;
9. работает просмотрщик `F3`;
10. работает простой редактор `F4`;
11. настройки и истории сохраняются между запусками;
12. проект собирается через `dotnet build`;
13. тесты проходят через `dotnet test`;
14. README описывает текущие возможности и ограничения.

## 23. Краткая формулировка задачи для первого запуска Codex / Claude Code

```text
Нужно начать реализацию проекта CSharpFar по ТЗ.

Выполни только Этап 0: создание solution и каркаса проекта.

Требования:
- C#;
- .NET 10;
- solution CSharpFar.sln;
- проекты CSharpFar.App, CSharpFar.Core, CSharpFar.Console, CSharpFar.FileSystem, CSharpFar.Shell, CSharpFar.Tests;
- nullable enabled;
- базовый README.md;
- .editorconfig;
- smoke test;
- dotnet build и dotnet test должны проходить.

Не реализовывай UI и файловый менеджер на этом этапе.
После завершения остановись и коротко опиши, что сделано.
```

## 24. Краткая формулировка задачи для продолжения после каждого этапа

```text
Продолжи реализацию CSharpFar по ТЗ.

Выполни только следующий этап: <номер и название этапа>.

Не забегай в последующие этапы.
После реализации:
- обнови README.md;
- добавь или обнови тесты, если применимо;
- запусти dotnet build;
- запусти dotnet test;
- остановись и опиши результат.
```
