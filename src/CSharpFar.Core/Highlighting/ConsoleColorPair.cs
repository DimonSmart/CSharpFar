namespace CSharpFar.Core.Highlighting;

/// <summary>Resolved console color pair used by the renderer after highlight is applied.</summary>
public sealed record ConsoleColorPair(int Foreground, int Background);
