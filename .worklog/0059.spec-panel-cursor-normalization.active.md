# 0059 SPEC Panel cursor normalization

Type: spec

Replaces:
- none

Related:
- 0012
- 0026
- 0030
- 0034

## Goal

Keep file panel cursor and scroll state valid after panel item lists are
refreshed, rebuilt, or sorted.

## Context

After the current file or directory disappears, `FilePanelState.CursorIndex` can
continue pointing past the end of `Items`. The renderer then has no row to
highlight, so the cursor appears to vanish. Deletion is only one source of this
state: refresh, watcher updates, external programs, virtual/search panel rebuilds
and sorting can all replace the item list.

The invariant belongs in panel control logic because cursor visibility depends
on the visible row count. `FilePanelState` should remain a state holder and the
renderer should only render the state it receives.

## Scope

1. Add a single `PanelController.NormalizeCursor` method that clamps
   `CursorIndex`, clamps or resets `ScrollOffset`, and keeps the cursor visible.
2. Use a shared restore helper after directory refresh and panel sorting.
3. Prefer restoring by item `Location`, then `FullPath`, then `Name`; if the old
   item disappeared, keep the previous numeric index and clamp it.
4. Normalize virtual and search result panels after sorting or refresh.
5. Add focused unit tests for refresh, sort, and direct normalization behavior.

## Non-goals

1. Do not change `DeleteCommand` or route this through
   `RefreshPanelsAfterFileOperation`.
2. Do not change selection pruning semantics.
3. Do not change `LoadDirectory` or `LoadLocation` cursor reset behavior when
   entering a new location.
4. Do not change panel load error handling, provider capabilities, or search
   results as a panel type.
5. Do not add correction logic to the renderer or to `FilePanelState` setters.

## Plan

1. Inspect current `PanelController`, `Application` virtual/search panel code,
   and existing panel controller tests.
2. Add `NormalizeCursor` and central restore logic in `PanelController`.
3. Replace directory refresh and sort cursor restoration with the shared helper.
4. Update virtual/search panel sorting and refresh paths to normalize after item
   list changes.
5. Add tests for removed current item, removed last item, parent-only list, empty
   list, preserved item, sort restore, sort clamp, and direct normalization.
6. Run repository checks.

## Pause points

- Stop before changing renderer behavior.
- Stop before changing delete/move/copy command semantics.
- Stop if preserving the previous item requires changing provider identity
  contracts beyond using existing `PanelLocation`, `FullPath`, and `Name`.

## Acceptance criteria

- Removing the last visible item leaves the cursor visible on the previous item.
- Removing an item in the middle leaves the cursor on the next item.
- If only `..` remains, cursor index is `0`.
- If the panel becomes empty, `CursorIndex` and `ScrollOffset` are both `0`.
- Refresh and sort do not leave `CursorIndex` outside `Items`.
- Sorting preserves the current item when it still exists.
- Search result and virtual panel sorting/refresh normalize cursor and scroll.
- Existing selection pruning behavior remains unchanged.

## Verification

Commands:

```bash
dotnet build
dotnet test
```

## Outcome

Implementation summary:
- Added `PanelController.NormalizeCursor` as the shared cursor/scroll invariant
  after item list changes.
- Replaced directory refresh and sort cursor restoration with a shared helper
  that restores by `Location`, then `FullPath`, then `Name`, and otherwise keeps
  the previous numeric index before normalizing.
- Updated search/virtual panel sorting and search result refresh rollback paths
  to normalize cursor and scroll explicitly.
- Updated reverse sort command routing for virtual panels to pass `visibleRows`
  into the sort/normalization contract.
- Added focused tests for removed middle item, removed last item, parent-only
  list, empty refresh, preserved item, sort restore, sort clamp, direct
  normalization, viewport scroll correction, and virtual-panel sort clamp.

Files changed:
- `src/CSharpFar.Core/Controllers/PanelController.cs`
- `src/CSharpFar.App/Application.cs`
- `src/CSharpFar.App/Commands/ApplicationCommandContext.cs`
- `src/CSharpFar.App/Commands/PanelToggleReverseSortCommand.cs`
- `tests/CSharpFar.Tests/PanelControllerTests.cs`
- `tests/CSharpFar.Tests/Spec012SearchResultsPanelTests.cs`
- `.worklog/0059.spec-panel-cursor-normalization.active.md`

Verification:
- `dotnet build` passed with 0 warnings and 0 errors.
- `dotnet test --no-build --filter "PanelControllerTests|Spec012SearchResultsPanelTests"` passed: 47 passed.
- `dotnet test --no-build --filter "Spec012SearchProgressDialogTests.Show_DoesNotConsumeInputAfterSearchCompletes"` passed: 1 passed.
- `dotnet test --no-build` was run twice and both runs failed only
  `CSharpFar.Tests.Spec012SearchProgressDialogTests.Show_DoesNotConsumeInputAfterSearchCompletes`
  with `No input events queued in FakeConsoleDriver`.

Deviations:
- The first targeted `dotnet test` was accidentally run concurrently with
  `dotnet build` and hit a transient DLL write lock. The subsequent build and
  targeted test runs were sequential.
- Full `dotnet test --no-build` did not pass because of the unrelated
  search-progress dialog test listed above. The same test passes in isolation.

Follow-up:
- Investigate the full-suite-only instability in
  `Spec012SearchProgressDialogTests.Show_DoesNotConsumeInputAfterSearchCompletes`
  separately from cursor normalization.
