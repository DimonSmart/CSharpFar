# 0045 SPEC Full JsonKit support

Type: spec

Replaces:
- 0044

Related:
- 0039
- 0040
- 0041
- 0042
- 0043

## Goal

Make `FarNet.JsonKit` 0.5.1 function as a real CSharpFar/FarNet module for
the features documented by the JsonKit README: opening JSON, browsing array and
object panels, opening nested values, editing values, deleting or nulling
items, saving JSON back to its source file, and using the module menu commands.

## Context

Spec `0044` proved the first real slice: the official JsonKit module can be
loaded, `jk:open file=<sample.json>` dispatches through the shim, and the root
JSON object is shown as a read-only CSharpFar panel.

Full JsonKit support requires more than command loading. JsonKit relies on
FarNet panel/explorer lifecycle methods, child panels, panel command/key
callbacks, content editing, content replacement, delete semantics, and some UI
services. CSharpFar should expose those through the native FarNet host model
instead of restoring the removed proxy plugin architecture.

## Scope

- Keep targeting `FarNet.JsonKit` 0.5.1 copied under
  `FarNet\Modules\JsonKit`.
- Implement FarNet panel navigation needed by `Enter` on JSON arrays and
  objects.
- Implement FarNet explorer content read/edit paths used by JsonKit `F4`.
- Implement JsonKit delete and set-null behavior for selected items:
  `Del`/`F8` and `ShiftDel`/`ShiftF8`.
- Implement panel command/key dispatch needed by JsonKit `CtrlS` save and its
  panel-local menu actions.
- Implement enough FarNet editor or editor-equivalent support for JsonKit value
  editing to update the backing JSON node.
- Implement enough clipboard/help/menu behavior for documented JsonKit menu
  items: Edit array of strings, Open from clipboard, Help.
- Add smoke tests or harness checks around real JsonKit operations, not only
  synthetic fixtures.
- Keep unsupported FarNet APIs explicit and diagnostic-driven.

## Non-goals

- Supporting other large FarNet modules in this spec unless their required API
  directly overlaps JsonKit.
- Implementing Far Manager macro compatibility beyond what JsonKit needs.
- Adding a security sandbox for FarNet modules.
- Replacing CSharpFar's native module domain with the old plugin proxy model.
- Supporting JsonKit versions newer than 0.5.1 without a separate compatibility
  check.

## Plan

1. Inventory JsonKit runtime blockers with smoke harness commands for:
   opening a root object, entering nested arrays/objects, editing a scalar,
   deleting, setting null, saving, and menu actions.
2. Extend the FarNet shim with the exact official method/type signatures needed
   by JsonKit panel operations: `OpenFileEventArgs`, `GetContentEventArgs`,
   `SetFileEventArgs`, `SetTextEventArgs`, `PanelEventArgs`,
   `KeyEventArgs`/`KeyInfo`, and panel command/key callbacks.
3. Extend `FarNetPanelAdapter` with a FarNet-aware interface for native panel
   operations that do not fit plain `IFilePanelSource`: open child panel, edit
   current file, delete selected files, set selected files to null, save panel,
   and invoke panel menu actions.
4. Route CSharpFar `Enter`, `F4`, `F8`/`Del`, `ShiftF8`/`ShiftDel`, and `CtrlS`
   to the FarNet adapter when the active source is a FarNet panel; keep normal
   behavior for local/SFTP/FTP panels.
5. Implement a minimal editor bridge. Prefer CSharpFar's existing editor when
   possible, but add a callback path so saving editor content calls JsonKit's
   FarNet set-content API instead of only editing a temporary file.
6. Add real JsonKit smoke checks:
   - `jk:open file=<sample.json>` opens root object.
   - Enter on a nested array/object opens a child panel.
   - F4 edits a scalar and updates the panel item.
   - delete removes an item.
   - shift-delete sets an item to null.
   - CtrlS writes the changed JSON file.
   - menu Open from clipboard opens JSON text or a JSON file path when clipboard
     support is available in the test host.
7. Run `dotnet build CSharpFar.slnx`, `dotnet test CSharpFar.slnx`, and real
   JsonKit smoke harness checks.

## Pause points

- Stop if JsonKit editing requires a broad non-modal FarNet editor lifecycle
  that cannot be mapped safely to CSharpFar's current editor in a small patch.
- Stop if a required API would change CSharpFar's public module domain in a way
  that affects FTP/SFTP panels.
- Stop if full menu/clipboard support depends on platform APIs unavailable in
  tests; document the manual check instead of faking behavior in production.

## Acceptance criteria

- `FarNet.JsonKit` 0.5.1 is discovered and loaded through the CSharpFar-owned
  `FarNet.dll` shim.
- `jk:open file=<sample.json>` opens an object or array panel with no
  diagnostics.
- Enter on nested JSON arrays and objects opens the corresponding child panel.
- F4 editing updates JSON values in the panel.
- Del/F8 removes selected JSON items.
- ShiftDel/ShiftF8 sets selected JSON items to `null`.
- CtrlS saves modified JSON back to the source file.
- JsonKit menu actions either work or fail with explicit documented unsupported
  diagnostics; the final outcome records any remaining gap.
- `dotnet build CSharpFar.slnx` and `dotnet test CSharpFar.slnx` pass.

## Verification

Commands or manual checks:

```bash
dotnet build CSharpFar.slnx
dotnet test CSharpFar.slnx
dotnet run --project artifacts\FarNetSmokeHarness\FarNetSmokeHarness.csproj -- src\CSharpFar.App\bin\Debug\net10.0\FarNet\Modules C:\Private\CSharpFar JsonKit "jk:open file=C:\Private\CSharpFar\artifacts\JsonKitSample.json"
```

## Outcome

Implementation summary:
- Extended the FarNet compatibility shim with menu item models, key models,
  panel current/selected file state, panel update/redraw/save hooks, panel key
  dispatch, clipboard methods, and `Far.Api.Window`.
- Added a CSharpFar-backed FarNet menu implementation through `Far.Api.CreateMenu`.
- Added Windows Unicode clipboard read/write support for FarNet clipboard APIs.
- Extended `FarNetPanelAdapter` so FarNet panels can open child panels, expose
  editable text, set edited text, handle panel-local keys, delete selected
  items, set selected items to null through Shift+Delete/Shift+F8, and save
  through Ctrl+S.
- Routed CSharpFar `Enter`, `F4`, `Del`/`F8`, `ShiftDel`/`ShiftF8`, and
  `CtrlS` to FarNet panel operations when the active panel source is a FarNet
  panel.
- Added a temporary-file editor bridge for FarNet scalar editing. Saving the
  temporary editor content calls the FarNet set-text path instead of editing a
  disconnected file.
- Added command-line text selection improvements needed by the interrupted
  work: Ctrl/Shift word movement, mouse selection, copy, paste, and right-click
  paste.
- Added same-volume drive selection behavior so choosing a volume can reuse the
  opposite panel's directory when it is already on that volume.
- Added a small FarNet menu dialog and UI service hook for module-created menus.
- Added mouse selection, mouse wheel movement, and scrollbar support to the
  FarNet menu dialog used by JsonKit menu actions.
- Implemented FarNet `ShowHelp` for `.hlf` files with topic extraction and a
  simple scrollable help viewer, so JsonKit Help no longer reports unsupported
  API failure.
- Added FarNet `IFar.GetFullPath(string)` so JsonKit Open from clipboard can
  resolve clipboard file paths without a missing-method failure.
- FarNet `ModuleHost` classes are now constructed and connected, which creates
  `JsonKit.Host.Instance` before JsonKit menu actions run.
- Kept unsupported FarNet APIs diagnostic-driven. The fixture unsupported-API
  test now uses `CreateListMenu`, because `CreateMenu` is supported by this
  implementation.

Files changed:
- `AGENTS.local.md`
- `src/CSharpFar.App/Application.cs`
- `src/CSharpFar.App/Commands/ApplicationCommandContext.cs`
- `src/CSharpFar.App/Commands/DriveSelectionCommand.cs`
- `src/CSharpFar.App/Commands/EditFileCommand.cs`
- `src/CSharpFar.Console/SystemConsoleDriver.cs`
- `src/CSharpFar.Console/Win32/Win32ConsoleApi.cs`
- `src/CSharpFar.Core/Models/CommandLineState.cs`
- `src/CSharpFar.FarNetCompat/FarApi.cs`
- `src/CSharpFar.FarNetCompat/FarModels.cs`
- `src/CSharpFar.FarNetHost/CSharpFarFarNetApi.cs`
- `src/CSharpFar.FarNetHost/FarNetPanelAdapter.cs`
- `src/CSharpFar.FarNetHost/IFarNetPanelOperations.cs`
- `src/CSharpFar.Ui/FarNetMenuDialog.cs`
- `src/CSharpFar.Ui/ModuleUiServices.cs`
- `src/CSharpFar.Ui/SingleLineTextInput.cs`
- `tests/CSharpFar.Tests.Fixtures.FarNetModule/FixtureTools.cs`
- `tests/CSharpFar.Tests/ApplicationVolumeTests.cs`
- `tests/CSharpFar.Tests/CommandLineStateTests.cs`
- `tests/CSharpFar.Tests/SingleLineTextInputTests.cs`
- `tests/CSharpFar.Tests/Spec027FarCommandLineShortcutTests.cs`
- `tests/CSharpFar.Tests/Spec039FarNetModuleToolLoaderTests.cs`

Deviations:
- FarNet `.hlf` rendering is intentionally plain text. It removes the blocking
  Help failure but does not emulate Far Manager help markup, links, or rich
  styling.
- Full JsonKit menu coverage was not manually exercised. The implementation now
  supports module-created menus and clipboard APIs needed by documented menu
  items, but remaining menu gaps should be treated as follow-up if they appear
  in real use.
- The editor bridge still uses the current CSharpFar file editor over a
  temporary file. It is intentionally not the new internal editor-domain model
  described by `0046`.
- Clipboard support is Windows-only in the FarNet API implementation. On other
  platforms the methods no-op or return empty text.
- `CreateListMenu` remains unsupported and is still covered by controlled
  failure tests.

Follow-up:
- Add a focused JsonKit menu smoke path for Open from clipboard and Edit array
  of strings.
- Add richer FarNet help rendering only if real modules need Far help links or
  markup beyond readable plain text.
- Replace the temporary-file editor bridge with the internal editor model once
  `0046` starts implementation.
- Decide whether command-line selection/copy/paste should be documented in a
  dedicated worklog if it becomes a user-visible feature area.

Verification:
- `dotnet test --filter Spec039FarNetModuleToolLoaderTests` passed: 9 passed.
- `dotnet test` passed: 731 passed, 1 skipped.
- `dotnet build` passed with 0 warnings.
- Real JsonKit smoke opened the root object panel:
  `jk:open file=C:\Private\CSharpFar\artifacts\JsonKitSample.json`.
- Real JsonKit smoke opened the nested `items` array and read scalar `1`.
- Real JsonKit smoke edited scalar `1` to `42`, ran CtrlS, and verified the
  temporary JSON file was saved with `42`.
- Real JsonKit smoke used ShiftDelete to set `1` to `null`, Delete to remove
  `2`, ran CtrlS, and verified the temporary JSON file was saved as
  `[null, 3]`.
- `dotnet build CSharpFar.slnx` passed with 0 warnings after the menu/help
  fixes.
- Focused tests passed:
  `dotnet test CSharpFar.slnx --no-build --filter "FarNetMenuDialogTests|Spec039FarNetModuleToolLoaderTests"`.
- Focused tests passed after the GetFullPath/ModuleHost fixes with 14 passed:
  `dotnet test CSharpFar.slnx --no-build --filter "FarNetMenuDialogTests|Spec039FarNetModuleToolLoaderTests"`.
- JsonKit menu Open from clipboard passed with clipboard text `{"items": [1, 2]}`.
- JsonKit menu Help passed through the smoke harness using
  `input=down,enter,escape`.
- JsonKit command smoke passed:
  `dotnet run --project artifacts\FarNetSmokeHarness\FarNetSmokeHarness.csproj --no-build -- src\CSharpFar.App\bin\Debug\net10.0\FarNet\Modules C:\Private\CSharpFar JsonKit "jk:open file=C:\Private\CSharpFar\artifacts\JsonKitSample.json"`.
- An earlier full `dotnet test CSharpFar.slnx --no-build` run hit one unrelated
  intermittent failure in
  `Spec012SearchProgressDialogTests.Show_DoesNotConsumeInputAfterSearchCompletes`;
  rerunning that single test passed, and the full suite excluding that test
  passed with 755 passed and 1 skipped.
- A later full `dotnet test CSharpFar.slnx --no-build` passed with 758 passed
  and 1 skipped after the FarNet fixes.
