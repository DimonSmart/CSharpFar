# 0036 SPEC Application icon

Type: spec

Replaces:
- none

Related:
- none

## Goal

Give CSharpFar its own Windows application icon with a Far-like file manager
identity without copying Far Manager branding or artwork.

## Context

The executable currently has no project icon configured. The application should
look identifiable in Windows shells, task switchers, and published artifacts.

## Scope

- Add an original icon asset for CSharpFar.
- Use a compact, high-contrast design that suggests a two-panel console file
  manager.
- Include icon sizes appropriate for a Windows executable.
- Configure the application project to embed the icon into the produced
  executable.

## Non-goals

- Do not copy the Far Manager icon, logo, exact layout, or trademarked visual
  details.
- Do not add installer branding, file associations, shortcuts, or packaging
  changes.
- Do not change runtime UI rendering or application behavior.
- Do not add compatibility paths around icon generation.

## Plan

1. Inspect the application project for existing resource settings.
2. Create a deterministic icon source and generated `.ico` asset.
3. Configure `CSharpFar.App.csproj` to use the icon.
4. Build the repository and verify the executable contains the icon resource.

## Pause points

- Stop if the project already has a separate packaging system that owns Windows
  branding.

## Acceptance criteria

- The repository contains the icon source and `.ico` asset.
- `CSharpFar.App` embeds the icon into `CSharpFar.exe`.
- `dotnet build` succeeds.
- The compiled executable exposes an icon resource.

## Verification

Commands:

```bash
dotnet build
dotnet test
```

Manual checks:

1. Inspect the generated executable icon in the output directory.

## Outcome

Implementation summary:
- Added `src/CSharpFar.App/Assets/CSharpFar.icon.svg` as the editable source
  for an original Far-like two-panel console icon.
- Added `src/CSharpFar.App/Assets/CSharpFar.ico` with 16, 20, 24, 32, 40, 48,
  64, 128, and 256 pixel images.
- Configured `CSharpFar.App.csproj` with `ApplicationIcon` so the Windows apphost
  executable embeds the icon.

Files changed:
- `.worklog/0036.spec-application-icon.active.md`
- `src/CSharpFar.App/Assets/CSharpFar.icon.svg`
- `src/CSharpFar.App/Assets/CSharpFar.ico`
- `src/CSharpFar.App/CSharpFar.App.csproj`

Verification:
- `dotnet build` passed with 0 warnings.
- `dotnet test` passed: 665 passed, 1 skipped.
- Parsed the `.ico` directory and confirmed 9 embedded sizes.
- Extracted the associated icon from `CSharpFar.exe` successfully.
- Visually checked the 256 pixel icon frame.

Deviations:
- None.

Follow-up:
- None.
