# 0024 SPEC Unused leftover cleanup

Type: spec

Replaces:
- none

Related:
- 0023

## Goal

Remove low-risk unused leftovers identified by architecture review 0023 without
changing production behavior.

## Context

Architecture review 0023 found a first cleanup group that is small enough to
verify independently: unused panel state, unused settings fields, and a
template-like smoke test filename.

## Scope

- Remove unused `FilePanelState.FilterMask`.
- Remove unused `AppSettings.Ui.Theme`.
- Remove unused `AppSettings.Shell.Kind`.
- Rename the generic `UnitTest1.cs` smoke test file to a purpose-revealing name.

## Non-goals

- Do not change runtime behavior.
- Do not add settings compatibility code.
- Do not extract shared dialog/list helpers.
- Do not change the panel provider or search-result source model.

## Plan

1. Confirm the listed fields have no source or test usage outside declarations.
2. Remove the unused fields from the model classes.
3. Rename the smoke test file without changing its assertions.
4. Run repository checks.

## Pause points

- Stop if any listed field has runtime usage that would make removal a behavior
  change.

## Acceptance criteria

- The codebase has no references to `FilterMask`, `Ui.Theme`, or `Shell.Kind`.
- The smoke tests are in a file with a purpose-revealing name.
- Repository checks pass.

## Verification

Commands:

```bash
dotnet build
dotnet test
```

## Outcome

Implementation summary:
- Removed unused `FilePanelState.FilterMask`.
- Removed unused `AppSettings.Ui.Theme` and `AppSettings.Shell.Kind`.
- Removed the obsolete `kind` field from the JSON settings load fixture.
- Renamed `tests/CSharpFar.Tests/UnitTest1.cs` to
  `tests/CSharpFar.Tests/CoreModelSmokeTests.cs` and renamed the test class to
  match.

Deviations:
- None.

Follow-up:
- Continue the remaining architecture review 0023 recommendations in separate,
  focused work items.

Verification:
- `dotnet build` passed with 0 warnings and 0 errors.
- `dotnet test` passed: 564 passed, 1 skipped, 565 total.
