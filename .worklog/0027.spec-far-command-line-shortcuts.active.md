# 0027 SPEC Far command-line shortcuts

Type: spec

Replaces:
- none

Related:
- 0013
- 0018
- 0026

## Goal

Add Far-like command-line insertion shortcuts for panel items, panel paths, and
command history navigation.

## Context

CSharpFar already has a command line, panel navigation, and command history. Far
Manager users expect several common `Ctrl` shortcuts to place panel-derived text
or recent commands into the command line without opening a dialog.

## Scope

Implement these key bindings when the main file manager UI is active:

1. `Ctrl+Enter` inserts the file or folder name under the cursor into the
   command line.
2. `Ctrl+F` inserts the full file path for the item under the cursor in the
   active panel.
3. `Ctrl+[` inserts the current full path of the right panel.
4. `Ctrl+]` inserts the current full path of the left panel.
5. `Ctrl+E` replaces the command line with the previous command from history.
   Repeated presses continue moving older through history.
6. `Ctrl+X` replaces the command line with the most recent command from history
   on first press. Repeated presses continue moving newer through history once
   navigation has moved to older commands.

Inserted names and paths must be quoted when the inserted text contains spaces.
History navigation through `Ctrl+E` and `Ctrl+X` must replace any currently typed
command-line text.

## Non-goals

Do not implement in this spec:

1. configurable keyboard shortcuts;
2. shell-specific escaping beyond quoting inserted text that contains spaces;
3. file-name completion or path completion;
4. persistent history changes beyond using the existing command history;
5. changes to function-key bar labels.

## Plan

1. Locate current main key routing, command-line editing, active/passive panel
   access, and command history browsing state.
2. Add small command-line insertion helpers for panel item names, item full
   paths, and panel current paths.
3. Route `Ctrl+Enter`, `Ctrl+F`, `Ctrl+[`, and `Ctrl+]` to those helpers.
4. Route `Ctrl+E` and `Ctrl+X` through command history navigation that replaces
   the entire command line and preserves repeat direction semantics.
5. Add focused tests for shortcut routing, quoting, panel side selection, and
   history replacement/cycling.

## Pause points

- Stop before changing low-level console input parsing if existing key events
  cannot represent one of the requested chords.
- Stop before changing command history storage semantics.
- Stop before adding shell escaping rules beyond the requested quote-on-spaces
  behavior.

## Acceptance criteria

- `Ctrl+Enter` appends the current item name to the command line.
- `Ctrl+F` appends the current active-panel item full path to the command line.
- `Ctrl+[` appends the right panel current path.
- `Ctrl+]` appends the left panel current path.
- Inserted text containing spaces is wrapped in quotes.
- Inserted text without spaces is not wrapped in quotes.
- `Ctrl+E` replaces existing command-line text with the most recent history
  command on first press and then older commands on repeated presses.
- `Ctrl+X` replaces existing command-line text with the most recent history
  command on first press and with newer history commands when navigation is
  already active.
- Existing command execution, `Alt+F8`, visible history completion, hidden-panel
  Up/Down browsing, and panel navigation keep working.

## Verification

Commands:

```bash
dotnet build
dotnet test
```

Manual checks:

1. Put the cursor on a file and press `Ctrl+Enter`; verify the file name appears
   in the command line.
2. Put the cursor on a file in a path with spaces and press `Ctrl+F`; verify the
   full path is quoted.
3. Press `Ctrl+[` and `Ctrl+]`; verify they insert the right and left panel
   paths respectively.
4. Type arbitrary command-line text, press `Ctrl+E`, then press `Ctrl+E` and
   `Ctrl+X` repeatedly; verify the line is replaced and history moves in both
   directions.

## Outcome

Implementation summary:
- Added Far-like command-line shortcuts in `Application`:
  `Ctrl+Enter`, `Ctrl+F`, `Ctrl+[`, `Ctrl+]`, `Ctrl+E`, and `Ctrl+X`.
- Added quote-on-spaces insertion for panel item names, active-panel item full
  paths, and explicit left/right panel current paths.
- Generalized command history browsing state so hidden-panel Up/Down and visible
  `Ctrl+E`/`Ctrl+X` use one predictable navigation index.
- Added `CommandLineState.InsertText` for cursor-position string insertion.
- Added focused spec tests for item-name insertion, active-panel full-path
  insertion, left/right panel path insertion, quote behavior, and history
  replacement/cycling.

Files changed:
- `.worklog/0027.spec-far-command-line-shortcuts.active.md`
- `src/CSharpFar.App/Application.cs`
- `src/CSharpFar.App/Commands/ApplicationCommandContext.cs`
- `src/CSharpFar.App/Commands/CommandHistoryCommand.cs`
- `src/CSharpFar.Core/Models/CommandLineState.cs`
- `tests/CSharpFar.Tests/CommandLineStateTests.cs`
- `tests/CSharpFar.Tests/Spec027FarCommandLineShortcutTests.cs`

Verification:
- `dotnet build` passed with 0 warnings and 0 errors.
- `dotnet test` passed: 579 passed, 1 skipped, 580 total.

Deviations:
- The shortcuts are routed before hidden-panel command-line handling, so they
  also work while panels are hidden. Hidden-panel Up/Down history browsing keeps
  its existing first-Down-from-empty behavior.
- Manual console QA was not run in this pass.

Follow-up:
- Run manual checks in a real Windows console for `Ctrl+[`, `Ctrl+]`, and
  `Ctrl+Enter`, because bracket-key reporting can vary by terminal.
