# 0056 - Built-in cd panel navigation

Related:
- 0016
- 0017
- 0018
- 0022

## Goal

Handle `CD` and `CHDIR` typed in the built-in command line as CSharpFar built-in
commands and navigate the active local file panel to the requested directory.

## Context

Typed external commands already run from the active panel directory in the current
console. `CD` and `CHDIR` are different from ordinary external commands: Far Manager
handles them as built-in commands instead of passing them to `%ComSpec%`.

CSharpFar should follow that model. A child shell process cannot change CSharpFar's
current directory or active panel location. For `cd abc`, CSharpFar must parse the
built-in command, resolve the target relative to the active panel directory, validate
it, and load it into the active local panel.

Likewise, on Windows a child `cmd.exe` cannot persist `set` assignments in the
CSharpFar process. The built-in command line must apply unambiguous `set`
assignments to CSharpFar's process environment so later child processes inherit
them.

## Scope

- Run ordinary typed shell commands from the active panel directory as before.
- Recognize standalone `CD` and `CHDIR` commands before shell dispatch.
- Do not recognize built-in `CD` or `CHDIR` when a registered explicit shell
  prefix selects a shell; in that case the shell receives the command body.
- Explicit shell prefix parsing is independent of built-in command parsing. A
  selected shell identity is executed as selected and is not silently replaced
  by the configured default shell.
- Resolve the requested directory relative to the active local panel directory.
- If the target directory exists, differs from the active panel directory, and can be
  opened by the local panel source, load it into the active panel.
- Support relative paths, subdirectories, absolute paths, quoted paths with spaces,
  and `/d` drive-switch syntax.
- Leave the panel unchanged when the command is not `CD`/`CHDIR`, the target is
  missing, or the target is invalid/inaccessible.
- Leave the panel unchanged when the active panel no longer has an available
  registered local source. In that case the built-in command must stop before
  physical path normalization or existence checks.
- Handle invalid or inaccessible final paths without crashing.
- Add focused tests for built-in `CD`/`CHDIR` panel navigation.
- On Windows, recognize unambiguous, case-insensitive `set NAME=value` assignments,
  including the quoted `set "NAME=value"` form. Leading whitespace is allowed;
  values may contain spaces and additional `=` characters.
- Set a non-empty value in CSharpFar's process environment and remove the variable
  when the value is empty. The change lasts only for the current CSharpFar process.
- Keep `set`, `set NAME`, `set /a`, `set /p`, malformed assignments, and all
  non-Windows `set` commands as ordinary shell commands.

## Non-goals

- Do not attempt to read the current directory of a child shell process.
- Do not make external commands such as `pushd` change the panel after they exit.
- Do not make the parent CSharpFar process current directory follow the panel.
- Do not add compatibility behavior for the old unsynchronized panel state.
- Do not change module command dispatch or non-local panel behavior.
- Do not add a persistent shell session.
- Do not modify User or Machine environment variables, write the registry, or use
  `setx`.
- Do not implement Unix-shell assignment syntax such as `export` in this behavior.

## Acceptance criteria

- `cd ..` moves the active panel one directory up.
- `cd someFolder` moves the active panel into the existing subdirectory.
- `cd "folder with spaces"` moves the active panel into the directory with spaces.
- `cd /d D:\SomeFolder` changes the panel drive and directory when that drive and
  directory exist.
- `chdir someFolder` behaves like `cd someFolder`.
- `ps: cd C:\\Temp` is sent to PowerShell and leaves the active panel unchanged.
- Explicit PowerShell prefixes `ps:`, `pwsh:`, and `powershell:` use the
  registered PowerShell profile. Windows prefers `pwsh.exe` then
  `powershell.exe`; Unix uses `pwsh`. If no candidate is available, the
  application reports that controlled error without terminating.
- `dir` runs through the configured shell and leaves the panel unchanged.
- An erroneous `cd` leaves the panel unchanged.
- Commands such as `pushd D:\Work` are ordinary shell commands and do not move the
  panel after the child process exits.
- The implementation does not attempt to read child-process cwd.
- Invalid or inaccessible final paths do not crash the application.
- On Windows, `set IDD_FACTORY_MODEL=gpt-5.6-luna` followed by
  `echo %IDD_FACTORY_MODEL%` outputs `gpt-5.6-luna` without restarting CSharpFar.
- `set NAME=` removes `NAME` from the CSharpFar process environment.
- Quoted assignments, values with spaces, and values with additional `=` characters
  retain their intended names and values.
- Environment display and special `set` forms continue through the configured shell.

## Verification

Manual checks:

1. Run `cd ..` from the built-in command line and confirm the active panel moves up.
2. Run `cd "folder with spaces"` and confirm the active panel opens that folder.
3. Run `cd /d D:\SomeFolder` when such a folder exists and confirm the active panel
   changes drive and directory.
4. Run `dir` and confirm the panel stays in place.
5. Run `cd C:\FolderThatDoesNotExist` and confirm the panel stays in place.
6. Run `ps: cd C:\Temp` and confirm the active panel stays in place.
7. On each supported platform, run an explicit PowerShell command and verify it
   uses the documented executable candidate order or reports a readable not-found error.
8. On Windows, set a variable, then run a separate `echo %NAME%` command and verify
   the value is inherited. Repeat with an empty value and verify it is absent.
9. Verify quoted values and values containing additional `=` are retained; verify
   `set`, `set NAME`, `set /a`, and `set /p` retain normal shell behavior.

## Source history

Imported from worklog/0056.spec-built-in-cd-panel-navigation.md.


