# 0007 - File panel options

## Context

File panels expose Far-like display and behavior options. These options affect
which items are visible, how they are sorted, what summary information appears,
and how mouse and refresh behavior work.

## Scope / Behavior

- The panel can show or hide hidden and system files.
- File highlighting can be enabled or disabled.
- Folder selection behavior is configurable so selection commands either include
  or exclude folders according to the option.
- Folder sorting can either group folders in the default way or sort folders by
  extension when that option is enabled.
- Panels can optionally remember the most recent local filesystem directory for
  the left and right sides independently and restore those directories on the
  next normal startup.
- Search results, module/plugin panels, and other non-local panel sources do not
  replace the remembered local filesystem directory for that side.
- If a remembered directory is unavailable at startup, the configured start
  directory is used when valid; otherwise the normal current-directory fallback
  applies.
- Remembered directories are captured only after a normal session exits cleanly.
- Panels can show a status line, total information, and free-space information.
- Panels can show a scrollbar when item count exceeds visible rows.
- The current sort mode is represented by a visible sort-mode letter where the
  display mode supports it.
- A parent directory entry can be shown at filesystem root when the option
  requires it, but it must not navigate above the real root.
- Panel root and parent navigation uses explicit panel path semantics rather
  than implicit parsing by the current operating system. Windows-style paths
  such as `C:\Root\Sub1`, Windows drive roots, UNC share roots, and Unix-style
  paths such as `/home/user/project` must be interpreted according to the
  panel path model being used, so tests for panel behavior are not dependent on
  the test runner operating system.
- Mouse input supports hit testing for panel rows, scrollbar areas, and item
  cells.
- Right-click selection behavior is configurable.
- Auto refresh updates panel contents after filesystem changes without losing
  cursor intent where possible.
- File-system location services expose current root and volume metadata used by
  panels and drive selection.
- Volume mount points are represented as navigable locations when supported.
- Settings persist these options across restarts.
- Ordinary user-facing panel options are edited in the unified
  `Options -> Settings... -> Panels` page rather than as separate checkbox
  commands in `Options`.
- The `Panels` page keeps one settings page and one scrolling/focus surface, but
  presents its controls as three compact, presentation-only groups in this
  order: `View`, `Files and folders`, `Interaction`.
- `View` contains the left/right panel view modes followed by status line, file
  count and total size, free disk space, and current sort mode indicator.
- `Files and folders` contains hidden/system visibility, file highlighting,
  folder extension sorting, and the filesystem-root `..` entry option.
- `Interaction` contains folder selection, right-click selection, and restoring
  the last opened panel folders on startup.
- Group headings and spacing are non-focusable presentation rows. They do not
  create settings, split keyboard navigation, introduce nested scrolling, or
  create separate apply/persistence paths.
- User-facing labels are descriptive while existing setting identifiers,
  control types, defaults, persistence fields, and runtime semantics remain
  unchanged.
- The settings dialog stages panel edits locally. Cancelling does not modify
  `AppSettings`, refresh panels, or persist settings.
- Saving applies all panel option values first, then performs one common panel
  refresh and one settings persistence operation. Legacy immediate-toggle
  commands may remain for compatibility but are not used by the unified dialog.

## Non-goals

- This spec does not require a complete clone of every Far Manager panel option.
- This spec does not require platform-neutral volume metadata beyond what the
  active platform backend can provide.
- This spec does not require mouse operation to replace keyboard operation.
- Remembering panel directories does not restore cursor position, selection,
  scroll position, search results, or non-local panel state.

## Acceptance criteria

- Changing panel options affects both rendering and panel behavior
  consistently.
- Hidden/system visibility, highlighting, folder selection, folder extension
  sorting, status rows, scrollbars, sort letters, root parent item behavior,
  mouse hit testing, right-click selection, and auto refresh have focused
  coverage.
- All ordinary panel settings previously exposed as `Options` checkboxes are
  available exactly once on the unified `Panels` settings page without
  duplicating file highlighting.
- The `Panels` page presents `View`, `Files and folders`, and `Interaction` in
  that order; group presentation never receives keyboard focus, and controls
  remain one continuous focus list with the standard page scrollbar.
- All controls remain keyboard reachable when the dialog is shorter than the
  page content, and moving focus scrolls the focused control into view.
- Panel settings changes have no runtime or persistence side effects before
  Save; Cancel discards them, while Save refreshes panels once after applying
  the complete set and persists once.
- When remembering panel directories is enabled, valid remembered local
  directories restore the left and right panels independently.
- Virtual and non-local panel sources never replace remembered local
  directories.
- When remembering panel directories is disabled, the existing configured
  start-directory behavior remains unchanged.
- An unavailable remembered directory falls back to the configured start
  directory and then to the current directory.
- Root parent item behavior and `GoToParent` navigation have focused coverage
  for both Windows and Unix panel path semantics.
- Settings persistence restores panel options after restart.
- Panel options do not corrupt cursor, selection, or scroll state when item
  visibility changes.

## Source history

Imported and translated from `worklog/0007.spec-file-panel-options.md`.

